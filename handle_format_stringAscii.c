#include "main.h"

/**
* handle_format_stringAscii - Handles the 'a' format specifier.
* @args: Argument list containing the string to print in ASCII format.
*
* Return: Number of characters printed.
*/
int handle_format_stringAscii(va_list args)
{
return (print_stringAscii(args));
}

