#include "main.h"

/**
* handle_format_p - Handles the 'p' format specifier.
* @args: Argument list containing the pointer to print.
*
* Return: Number of characters printed.
*/
int handle_format_p(va_list args)
{
return (print_p(args));
}

