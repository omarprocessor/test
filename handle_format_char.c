#include "main.h"

/**
* handle_format_char - Handles the 'c' format specifier.
* @args: Argument list containing the character to print.
*
* Return: Number of characters printed.
*/
int handle_format_char(va_list args)
{
return (print_char(args));
}

